/** @type {import('next').NextConfig} */
// This is a JSDoc comment specifying the type of the variable `nextConfig` as `import('next').NextConfig`.
// It indicates that `nextConfig` should adhere to the Next.js configuration interface.

const nextConfig = {}
// Declare a variable `nextConfig` and assign an empty object to it.
// This variable is intended to hold the Next.js configuration.

module.exports = nextConfig
// Export the `nextConfig` variable as the module export.
// This makes the `nextConfig` object available to be imported in other files using `require` or `import`.
